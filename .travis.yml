language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-pd-gulp' ]; then cd .. && eval "mv $currentfolder generator-pd-gulp" && cd generator-pd-gulp; fi
